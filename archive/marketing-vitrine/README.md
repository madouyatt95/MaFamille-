# Vitrine MyFamily+

Cette ancienne page de presentation est archivee hors de `src` et de `public`.
Elle n'est plus compilee, publiee ou accessible par une route de l'application.

Contenu conserve :

- `MarketingLanding.tsx` : ancienne page publique ;
- `MarketingDemoCapture.tsx` : ancien outil de demonstration ;
- `public/marketing-captures/` : medias utilises par la vitrine.

Depuis cet archivage, `/` et `/app` ouvrent tous les deux directement MyFamily+.
Les invitations de foyer utilisent explicitement `/app?join=CODE`.
